import {
    type FormulasInterface,
    type GendersInterface,
    type UnitsInterface,
} from '../interfaces'

export const gendersConst: GendersInterface = {
    male: 'Male',
    female: 'Female',
}

export const unitsConst: UnitsInterface = {
    metric: 'Metric',
    imperial: 'Imperial',
}

export const formulas: FormulasInterface = {
    devine: 'Devine',
    robinson: 'Robinson ',
}
